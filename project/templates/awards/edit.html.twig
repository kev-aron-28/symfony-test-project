{% extends 'base.html.twig' %}
  
{% block title %}Editar {% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit Project</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('awards') }}"> 
                    Distinciones al Mérito Politécnico
                </a>
            </div>
            <div class="card-body">
                {% include 'awards/_form.html.twig' with {'button_label': 'Update Project', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>
{% endblock %}
